## 概念卡 4：make 时间戳模型

**是什么**：make 会对比目标文件和它所有依赖文件的修改时间戳，只要依赖文件比目标文件新，就触发重新编译生成目标。
**为什么**：靠时间戳判断可以只重新编译改动过的文件，不用全量重编译，大幅提升大型项目的编译效率。
**我代码里的位置**：规则写在 Makefile 中，每一行 “目标：依赖列表” 的语句，都对应一套时间戳检查逻辑。
**易错点**：依赖清单必须人工维护完整，漏写头文件等依赖时，修改对应文件后 make 不会重编译，会静默生成错误的二进制文件。