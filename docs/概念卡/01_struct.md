## 概念卡 1：struct 蓝图 vs 实例

**是什么**：蓝图是 struct 的类型定义，只规定成员的种类和格式，相当于模具；实例是用该类型创建的具体变量，是真正存储数据的实体。
**为什么**：蓝图仅描述结构规则，不占用实际内存；只有生成实例后，系统才会按蓝图大小分配内存，存放真实数据。
**我代码里的位置**：struct 类型定义（蓝图）通常写在 .h 头文件中供复用；结构体变量（实例）在 .c 源文件里定义和使用。
**易错点**：把结构体类型名当成变量直接用；C 语言声明实例时漏写 struct 关键字；用 sizeof 时混淆类型和实例。