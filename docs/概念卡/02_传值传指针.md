## 概念卡 2：传值 vs 传指针

**是什么**：传值是把数据的副本传给函数，函数操作的是复制品；传指针是把数据的内存地址传进去，函数可以直接操作原数据。
**为什么**：传值不会修改原变量，更安全但大数据量时拷贝开销大；传指针能修改原数据、节省拷贝成本，但存在非法访问风险。
**我代码里的位置**：函数参数列表中，普通变量写法就是传值；参数带 `*` 指针符号的就是传指针。
**易错点**：误以为传值能修改原变量；传指针时忽略空指针校验；需要修改指针本身时错用了一级指针。