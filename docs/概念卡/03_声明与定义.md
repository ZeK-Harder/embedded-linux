## 概念卡 3：声明 vs 定义

**是什么**：声明是告诉编译器 “存在这个符号”，不分配内存；定义是实际创建实体、分配内存，是具体的实现本体。
**为什么**：.h 放声明可以重复包含，只是重复告知编译器规则，不会产生冲突；定义有实体内存，多份就会重复，链接期检查符号时就会撞名报错。
**我代码里的位置**：函数声明、extern 全局变量声明放在 .h 头文件；函数实现、全局变量定义都写在对应的 .c 源文件里。
**易错点**：在 .h 里写函数体或初始化全局变量，多文件包含后触发重定义；误以为编译期就会检查符号重名。